version: "3.9"
services:
  pyro:
    build: .
    environment:
      API_ID: $API_ID
      API_HASH: $API_HASH
      BOT_TOKEN: $BOT_TOKEN
      LOG_CHANNEL: $LOG_CHANNEL
      OWNER_ID: $OWNER_ID
      MONGODB_URI: $MONGODB_URI
